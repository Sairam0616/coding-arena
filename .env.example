# Copy this file to `.env` and fill in your own values.

MONGO_URL=mongodb+srv://hellosai13579_db_user:rU99YQNTlT5TPfx5@cluster0.p4alumq.mongodb.net/coding_arena?retryWrites=true&w=majority
DB_NAME=coding_arena
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Any OpenAI-compatible LLM key. The default endpoint in code is the Emergent proxy
# at https://integrations.emergentagent.com/llm/v1 — change BASE in app/api/[[...path]]/route.js
# if you want to use OpenAI / Anthropic directly.
EMERGENT_LLM_KEY=sk-...

# Generate with:
#   node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=replace-with-32-byte-hex

CORS_ORIGINS=*
